export enum UserType {
    TENANT = 'tenant',
    LANDLORD = 'landlord',
}

export enum DealStatus {
    REQUESTED = 'requested',
    CONFIRMED = 'confirmed',
    CANCELLED = 'cancelled',
}

export enum MessageStatus {
    SENT = 'sent',
    RECEIVED = 'received',
    READ = 'read',
}
